<menu xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto">
  <item
    android:id="@+id/menu_scan_toggle"
    android:title="@string/menu_start_scan"
    app:showAsAction="never" />
  <item
    android:id="@+id/menu_rescan"
    android:title="@string/menu_rescan"
    app:showAsAction="never" />
  <item
    android:id="@+id/menu_continuous_scanning"
    android:checkable="true"
    android:title="@string/continuous_scanning"
    app:showAsAction="never" />
  <item
    android:id="@+id/menu_diagnostics"
    android:title="@string/diagnostics"
    app:showAsAction="never" />
  <item
    android:id="@+id/menu_copy_diagnostics"
    android:title="@string/menu_copy_diagnostics"
    app:showAsAction="never" />
</menu>
